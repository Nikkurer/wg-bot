[project]
name = "wg-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.22.0",
    "httpx>=0.28.1",
    "pillow>=11.3.0",
    "pyyaml>=6.0.2",
    "qrcode>=8.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.4.0",
]
